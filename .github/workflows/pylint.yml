name: Pylint

on:
  push:
    paths: ["**/*.py"]
  pull_request:
    paths: ["**/*.py"]

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11" 

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pylint

#Testing app.py and test folder, add other files if needed
      - name: Run Pylint
        run: pylint app.py test
